services:
  xcind:
    build:
      context: .
      args:
        BASHVER: latest
    volumes:
      - .:/opt/xcind:ro
      - /var/run/docker.sock:/var/run/docker.sock
    working_dir: /opt/xcind
    entrypoint: ["bash"]
    command: ["test/test-xcind.sh"]
